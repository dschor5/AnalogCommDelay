version: 1
disable_existing_loggers: true

formatters:
    standard: 
        format: '%(asctime)s:%(name)s:%(levelname)s:%(message)s'

handlers:
    debug_file_handler:
        class: logging.handlers.RotatingFileHandler
        backupCount: 5
        maxBytes: 1048576 # 1 MB
        level: DEBUG
        formatter: standard
        filename: test/delay_server_test.log

root:
    level: NOTSET
    handlers: [debug_file_handler]
    propagate: yes
